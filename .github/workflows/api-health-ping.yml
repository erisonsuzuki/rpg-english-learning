name: API Health Ping

on:
  schedule:
    - cron: "*/14 5-23 * * *"
      timezone: "America/Sao_Paulo"
  workflow_dispatch:

jobs:
  ping-health:
    environment: financial-agent-system
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      NEXT_PUBLIC_SITE_URL: ${{ vars.NEXT_PUBLIC_SITE_URL }}

    steps:
      - name: Validate NEXT_PUBLIC_SITE_URL is configured
        run: |
          if [ -z "${NEXT_PUBLIC_SITE_URL}" ]; then
            echo "NEXT_PUBLIC_SITE_URL is not set (Settings > Secrets and variables > Actions)."
            exit 1
          fi

      - name: Ping /health
        run: |
          set -euo pipefail
          URL="${NEXT_PUBLIC_SITE_URL%/}/api/health"
          echo "Pinging ${URL}"
          curl --fail --silent --show-error \
            --max-time 15 \
            --retry 2 \
            --retry-delay 3 \
            "${URL}"
